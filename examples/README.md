# `examples/`

This folder will hold sample inputs for demos and tests.

## Planned Content

```text
examples/
  kb/       # small knowledge base files for CLI/API demos
  cli/      # command transcripts and expected output examples
  api/      # request examples for FastAPI endpoints
```

## Current Note

The existing sample knowledge base is still in `data/kb.txt`. It can move into `examples/kb/` in a later PR when ingestion behavior is stabilized.
